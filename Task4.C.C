#include <stdio.h>
int main()
{
    int dM, dW;
    printf("data: \n");
    printf("day of month: ");
    scanf("%d", &dM);
    printf("day number of the week: ");
    scanf("%d", &dW);  
    if (13 == dM || 17== dM);
        if (5 == dW || 2 == dW)
        printf("be careful! unlucky day");

    return 0;
}

